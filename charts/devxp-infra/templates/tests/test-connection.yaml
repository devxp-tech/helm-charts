apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "devxp-infra.fullname" . }}-test-connection"
  labels:
    {{- include "devxp-infra.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "devxp-infra.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
