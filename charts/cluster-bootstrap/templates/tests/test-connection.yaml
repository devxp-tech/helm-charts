apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cluster-bootstrap.fullname" . }}-test-connection"
  labels:
    {{- include "cluster-bootstrap.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cluster-bootstrap.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
